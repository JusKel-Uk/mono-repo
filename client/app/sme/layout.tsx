// The SME dashboard chrome lives in <DashboardShell>, applied per page, so this
// segment layout is a simple pass-through.
export default function SmeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
